import numpy as np

class solve:
    totalthesis = 38
    totalpartys = 28
    q = np.zeros(totalthesis,"uint8")
    m = np.zeros(totalthesis,"bool")
    def __init__(self,beliefs):
        self.beliefs = beliefs
        self.goal = goal
    
    def solve(self,goal):
        self.goal = goal
        
    
